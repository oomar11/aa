# قاعدة بيانات الورشة المشتركة (Postgres / Neon / Vercel Postgres)
# انسخ Connection string من Neon → حطه هنا باسم DATABASE_URL
DATABASE_URL=

# بدائل يقرأها البرنامج أيضاً إن وُجدت
# POSTGRES_URL=
# POSTGRES_PRISMA_URL=
# DATABASE_URL_UNPOOLED=
# NEON_DATABASE_URL=

# ربط خزنة المتجر يتم من داخل الإعدادات (رابط المتجر + WORKSHOP_BRIDGE_SECRET)
# لا حاجة لمتغير env هنا — الإعداد مشترك عبر Neon بين أجهزة الورشة
